import { useNavigate } from 'react-router-dom'
import { ArrowRightIcon, BarChart3Icon, BrainCircuitIcon, CheckIcon, FileTextIcon, MessageSquareTextIcon, ShieldCheckIcon } from 'lucide-react'
import { AppShell } from '@/components/AppShell'
import { Badge, Button, Card, CardContent } from '@/components/primitives'

const features = [{ icon: FileTextIcon, title: 'Resume-aware questions', text: 'Your skills and experience shape every prompt.' }, { icon: MessageSquareTextIcon, title: 'Natural interview flow', text: 'Answer one focused question at a time.' }, { icon: BarChart3Icon, title: 'Useful feedback', text: 'See what worked and what needs more practice.' }]
export default function HomePage() { const navigate = useNavigate(); return <AppShell><section className="hero"><div><p className="eyebrow"><BrainCircuitIcon size={14} /> Powered by IBM Granite</p><h1>Turn interview practice into <strong>real progress.</strong></h1><p className="lead">A focused, resume-aware space for technical and behavioural interview practice. Get clear feedback after every answer.</p><div className="hero__actions"><Button size="lg" onClick={() => navigate('/setup')}>Start practice interview <ArrowRightIcon size={17} /></Button><span className="note">No account required</span></div></div><Card className="preview"><CardContent><div className="preview__top"><div><p className="preview__label">Practice session</p><p className="preview__role">Software Engineer · Mixed</p></div><Badge variant="outline">Question 3 of 8</Badge></div><div className="preview__question"><p className="preview__label">Current question</p><p>How would you design an API that safely handles concurrent updates to a user profile?</p></div><div className="preview__feedback"><CheckIcon size={15} /> Feedback identifies strengths, gaps, and the next topic worth revisiting.</div></CardContent></Card></section><section className="feature-grid">{features.map(({ icon: Icon, title, text }) => <Card className="feature-card" key={title}><Icon size={23} /><h2>{title}</h2><p>{text}</p></Card>)}</section><section className="workflow"><div className="workflow__top"><div><p><b>A focused three-step session</b></p><p className="workflow__sub">Designed to make meaningful practice feel straightforward.</p></div><p className="workflow__sub"><ShieldCheckIcon size={15} /> Your answers stay in this session</p></div><div className="setup-steps">{['Upload your resume', 'Choose a role and format', 'Practice and review feedback'].map((item, index) => <div className="setup-step" key={item}><b>{index + 1}</b>{item}</div>)}</div></section></AppShell> }
